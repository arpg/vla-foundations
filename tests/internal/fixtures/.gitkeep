# Gold Standard Fixtures

This directory contains reference outputs for rigorous internal testing.

## Creating Gold Standards

1. Inject solution:
   ```bash
   python scripts/manage_solutions.py --inject scratch-1
   ```

2. Train model and save checkpoint:
   ```bash
   cd src/assignments/scratch-1
   python backbone.py
   ```

3. Generate gold standard output:
   ```python
   import torch
   from backbone import DecoderOnlyTransformer

   # Load trained model
   model = DecoderOnlyTransformer(...)
   model.load_state_dict(torch.load('checkpoints/best_model.pt'))
   model.eval()

   # Create test input
   test_input = torch.randn(1, 10, 384)

   # Generate output
   with torch.no_grad():
       output = model.projector(test_input)

   # Save as gold standard
   torch.save({
       'input': test_input,
       'output': output,
       'metadata': {
           'created': '2024-01-23',
           'model_config': {...}
       }
   }, 'tests/internal/fixtures/scratch1_gold_output.pt')
   ```

## Gold Standard Files

- `scratch1_gold_output.pt`: Expected projector output for fidelity testing
- (Add more as needed for each assignment)
