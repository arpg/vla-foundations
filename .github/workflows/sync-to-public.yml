name: Sync to Public Repo

on:
  push:
    tags:
      - 'release-scratch-*'   # Trigger on release tags like release-scratch-1, release-scratch-2, etc.

jobs:
  sanitize-and-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout private repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for proper sync and linting
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # =====================================================================
      # PRE-SYNC VALIDATION (FAIL-SAFE)
      # =====================================================================

      - name: Validate Git History (Senior Staff Form)
        run: |
          echo "=== Validating Git History ==="

          # Check for merge commits (non-linear history)
          MERGE_COMMITS=$(git log --merges --oneline | head -5)
          if [ ! -z "$MERGE_COMMITS" ]; then
            echo "⚠️  Warning: Found merge commits (non-linear history):"
            echo "$MERGE_COMMITS"
            echo ""
            echo "Recommendation: Use rebase workflow for cleaner history"
          fi

          # Check recent commit messages for semantic structure
          echo ""
          echo "Recent commits:"
          git log --oneline -5

          # Check for conventional commit format
          NON_SEMANTIC=$(git log --oneline -10 | grep -v -E "^[a-f0-9]+ (feat|fix|docs|style|refactor|test|chore|perf)(\(.+\))?:" || true)
          if [ ! -z "$NON_SEMANTIC" ]; then
            echo ""
            echo "ℹ️  Note: Some commits don't follow Conventional Commits format"
            echo "   Consider using: feat(), fix(), docs(), etc."
          fi

          echo ""
          echo "✅ Git history validation complete"

      - name: Pre-Sanitization Linting
        run: |
          echo "=== Pre-Sanitization Linting ==="

          # Run sanitization in dry-run mode first (with fail-safe checks)
          echo "Running sanitization validation..."
          python3 scripts/_sanitize_todos.py || {
            echo "❌ FAIL-SAFE: Sanitization pre-check failed!"
            echo "   Fix [SOLUTION] markers before creating release tag."
            exit 1
          }

          echo ""
          echo "✅ Pre-sanitization linting passed"

      - name: Display repository state
        run: |
          echo "=== Repository State Before Sanitization ==="
          echo "Current branch/tag: $(git describe --tags --always)"
          echo "Private directories:"
          ls -la private/ 2>/dev/null || echo "  (not found)"
          ls -la tests/internal/ 2>/dev/null || echo "  (not found)"
          echo ""

      # =====================================================================
      # SANITIZATION PIPELINE
      # =====================================================================

      - name: Run sanitization script
        run: |
          echo "=== Running Sanitization Pipeline ==="
          bash scripts/sanitize.sh

      - name: Display repository state after sanitization
        run: |
          echo "=== Repository State After Sanitization ==="
          echo "Remaining files:"
          git status
          echo ""
          echo "Changed files:"
          git diff HEAD~1 --name-only || echo "(no previous commit)"
          echo ""

      # =====================================================================
      # POST-SANITIZATION VALIDATION (FAIL-SAFE)
      # =====================================================================

      - name: Post-Sanitization Leak Detection
        run: |
          echo "=== FAIL-SAFE: Checking for Solution Leaks ==="

          LEAK_FOUND=0

          # Check for TODO: [SOLUTION] comments
          echo "1. Checking for TODO: [SOLUTION] patterns..."
          if grep -r "TODO:.*\[SOLUTION\]" src/ content/ 2>/dev/null; then
            echo "❌ ERROR: Found TODO: [SOLUTION] in sanitized code!"
            LEAK_FOUND=1
          else
            echo "   ✓ No TODO: [SOLUTION] found"
          fi

          # Check for any [SOLUTION] markers
          echo ""
          echo "2. Checking for [SOLUTION] markers..."
          if grep -r "\[SOLUTION\]" src/ content/ 2>/dev/null; then
            echo "❌ ERROR: Found [SOLUTION] markers in sanitized code!"
            LEAK_FOUND=1
          else
            echo "   ✓ No [SOLUTION] markers found"
          fi

          # Check that private/ is gone
          echo ""
          echo "3. Verifying private/ removal..."
          if [ -d "private/" ]; then
            echo "❌ ERROR: private/ directory still exists!"
            LEAK_FOUND=1
          else
            echo "   ✓ private/ removed"
          fi

          # Check that tests/internal/ is gone
          echo ""
          echo "4. Verifying tests/internal/ removal..."
          if [ -d "tests/internal/" ]; then
            echo "❌ ERROR: tests/internal/ directory still exists!"
            LEAK_FOUND=1
          else
            echo "   ✓ tests/internal/ removed"
          fi

          # Check that dev utilities script is gone
          echo ""
          echo "5. Verifying dev_utils.py removal..."
          if [ -f "scripts/dev_utils.py" ]; then
            echo "❌ ERROR: dev_utils.py still exists!"
            LEAK_FOUND=1
          else
            echo "   ✓ dev_utils.py removed"
          fi

          # Check that dev scripts are gone
          echo ""
          echo "6. Verifying scripts/dev/ removal..."
          if [ -d "scripts/dev/" ]; then
            echo "❌ ERROR: scripts/dev/ directory still exists!"
            LEAK_FOUND=1
          else
            echo "   ✓ scripts/dev/ removed"
          fi

          echo ""
          if [ $LEAK_FOUND -eq 0 ]; then
            echo "✅ LEAK DETECTION PASSED: Repository is clean"
          else
            echo "❌ LEAK DETECTION FAILED: Solution content remains!"
            echo ""
            echo "🚫 ABORTING SYNC - Fix sanitization issues"
            exit 1
          fi

      # =====================================================================
      # PUSH TO PUBLIC REPOSITORY (SAFE MERGE STRATEGY)
      # =====================================================================
      #
      # Uses a safe merge strategy that:
      # 1. Fetches the current public repo state
      # 2. Creates a new branch with sanitized content
      # 3. Merges into public main (preserving student branches)
      # 4. Only updates content files, doesn't destroy history
      #

      - name: Push to public repo (Safe Merge Strategy)
        env:
          PUBLIC_REPO_TOKEN: ${{ secrets.PUBLIC_REPO_TOKEN }}
        run: |
          echo "=== Pushing to Public Repository (Safe Merge Strategy) ==="
          echo ""

          # Verify token is available
          if [ -z "$PUBLIC_REPO_TOKEN" ]; then
            echo "❌ ERROR: PUBLIC_REPO_TOKEN is not set!"
            echo "Please add the token to repository secrets:"
            echo "  Settings > Secrets and variables > Actions > New repository secret"
            exit 1
          fi
          echo "✓ Token is available (length: ${#PUBLIC_REPO_TOKEN})"
          echo ""

          # Configure git
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          # Configure git credential helper to use the token
          git config --global credential.helper store
          echo "https://x-access-token:${PUBLIC_REPO_TOKEN}@github.com" > ~/.git-credentials

          # Get the tag name for release message
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Release tag: $TAG_NAME"
          echo ""

          # Add public remote (without embedding credentials in URL for security)
          git remote add public https://github.com/arpg/vla-foundations.git

          # Fetch public repo
          echo "Fetching public repository..."
          git fetch public main

          # Create a fresh branch for the sanitized content
          echo "Creating release branch..."
          git checkout -b release-${TAG_NAME}

          # Stage all sanitized files
          git add -A

          # Commit the sanitized content
          RELEASE_DATE=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          git commit -m "Release: ${TAG_NAME} (${RELEASE_DATE})" \
                     -m "" \
                     -m "Sanitized content from private repository." \
                     -m "This release includes updated assignment materials." \
                     -m "" \
                     -m "Changes:" \
                     -m "- Updated assignment templates" \
                     -m "- Fixed bugs and improvements" \
                     -m "- Documentation updates"

          # Checkout public main
          echo "Checking out public main..."
          git checkout -B public-main public/main

          # Merge release into public-main (using theirs strategy for content files)
          echo "Merging release content..."
          git merge release-${TAG_NAME} --no-edit -X theirs --allow-unrelated-histories || {
            echo "Merge had conflicts, resolving with release content..."
            git checkout --theirs .
            git add -A
            git commit -m "Merge release ${TAG_NAME}: resolved with release content"
          }

          # Push merged main back to public
          echo ""
          echo "Pushing to public repository main branch..."
          git push public public-main:main

          echo ""
          echo "✅ Successfully synced to public repository"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Release Details:"
          echo "  Tag: $TAG_NAME"
          echo "  Date: $RELEASE_DATE"
          echo "  Strategy: Safe Merge (preserves student branches)"
          echo ""
          echo "Public Repository:"
          echo "  URL: https://github.com/arpg/vla-foundations"
          echo "  Branch: main"
          echo "  Status: Live ✓"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
